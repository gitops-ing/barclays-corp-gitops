apiVersion: tf.upbound.io/v1beta1
kind: Workspace
metadata:
  name: test-catalog-bootstrap
spec:
  providerConfigRef: 
    name: test-catalog
  forProvider:
    source: Remote
    module: git::https://github.com/gitops-ing/barclays-corp-gitops.git//terraform/aws/modules/bootstrap?ref=main
    vars:
    - key: cluster_name
      value: "test-catalog"
