apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: test-catalog-bootstrap
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: '15'
spec:
  project: default
  source:
    repoURL: https://github.com/gitops-ing/barclays-corp-gitops
    path: registry/clusters/test-catalog/infrastructure-bootstrap
    targetRevision: HEAD
  destination:
    name: in-cluster
    namespace: test-catalog
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 30
      backoff:
        duration: 1m
        factor: 1
