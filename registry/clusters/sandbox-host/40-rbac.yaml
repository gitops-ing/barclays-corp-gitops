apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sandbox-host-rbac
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  annotations:
    argocd.argoproj.io/sync-wave: '40'
spec:
  project: sandbox-host-platform
  source:
    repoURL: https://github.com/gitops-ing/barclays-corp-gitops
    path: registry/clusters/sandbox-host/rbac
    targetRevision: HEAD
  destination:
    name: sandbox-host
    namespace: rbac
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
